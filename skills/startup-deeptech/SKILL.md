---
name: startup-deeptech
description: >
  Use when writing deep-tech use cases and tier-1 diagnostic/algorithm
  writeups, and when generating the first live product-screen images that lock
  the visual language. Trigger on /startup-deeptech, "deep tech use cases",
  "how the AI actually works."
---

# startup-deeptech

Replay source EX10 and EX14. This phase **locks visual spec**.

## Writes

`06_deeptech/USE_CASES.md` — at least six named mechanisms. Source set (domain-swap):

1. Recursive multimodal state-boundary probing + real-time load adaptation
2. Hybrid graph for simultaneous repair + acceleration
3. Dynamic multi-representational delivery engine
4. Suitability scoring + multi-agent verification
5. Durable multi-resolution memory + forgetting-aware retrieval
6. Buyer/regulator evidence + oversight dashboard

`06_deeptech/ALGORITHMS.md` — tier-1 diagnostics unpacked (algorithms, inputs, update rules, failure modes, trade-offs).

## Images (S01, 4 screens)

Generate **now**, four live product screens (see `references/visual-spec.md` family A). Each screen makes one mechanism visible as UI, not a poster. Filenames: `images/S01_01.png` … `S01_04.png`.

Inspect each. If it looks like a marketing laptop mock, redo.
