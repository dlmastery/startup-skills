# D07 — Integrations & Ecosystem

What the loop plugs into. Retrieval is commodity infrastructure [B24][B25]; the value is enforced correct use (Gate 2), not the API calls.

```mermaid
flowchart TD
    subgraph IN["Data in"]
        CSV["csv / parquet / numpy [exists]"]
        SKL["sklearn dataset fetchers [exists]"]
        CONN["Warehouse connectors:<br/>Postgres, BigQuery, S3 [planned]"]
    end

    subgraph LITAPIS["Literature APIs"]
        ARX["arXiv API [exists in loop practice]"]
        S2["Semantic Scholar Graph [planned wrapper]"]
        OA["OpenAlex - free, no key [planned wrapper]"]
    end

    subgraph CORE["Ascent core loop"]
        LOOP["7-step loop + gates"]
        CAT["SOTA catalog<br/>per-fork yaml [exists]<br/>subscription feed [planned]"]
        GATE2["Citation-rigor gate [exists]<br/>live-index existence check [planned]"]
    end

    subgraph VCS["Version control"]
        GIT["git - commit per kept experiment,<br/>hash in ledger row [exists]"]
    end

    subgraph OUT["Evidence out"]
        DASH["Local HTML dashboard [exists]"]
        COLAB["Colab notebook per champion [exists]"]
        NB["Jupyter inference kit - predict.py [exists]"]
        MLF["Export to MLflow / W&B formats [planned]"]
        CI["Reproduction webhook - rerun bundle in CI [planned]"]
    end

    IN --> LOOP
    LITAPIS --> CAT
    LITAPIS --> GATE2
    CAT --> LOOP
    GATE2 --> LOOP
    LOOP --> GIT
    LOOP --> DASH
    LOOP --> COLAB
    LOOP --> NB
    LOOP --> MLF
    LOOP --> CI
```

**What a reviewer should notice:** (1) the integration surface is deliberately asymmetric — thin on the way in (files and fetchers; a laptop tool must not require a data platform), rich on the way out (every champion exports a runnable bundle: Colab notebook, predict.py, reproduce log — the evidence formats skeptics already trust); (2) git is not decoration: one commit per kept experiment with the hash in the ledger row is what makes the champion lineage independently checkable; (3) exporting *to* MLflow/W&B positions trackers as consumers of Ascent's records, not competitors (landscape Tier 5: trackers record what humans chose to run — Ascent generates the record); (4) the scholarly APIs are free and stable — no integration moat is claimed there, per capability_table §4.
