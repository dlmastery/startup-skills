# D10 — Human-in-the-Loop Steering & Escalation

The stall → self-reflect → pivot ladder (constitution §49–52). The protocol and steering log exist; the detector and notification surface are productization. This is the diagram of "minimum steering bandwidth" — the open question the literature hasn't measured (survey §6) that Ascent's steering log turns into a metric.

```mermaid
stateDiagram-v2
    [*] --> Productive
    Productive : Productive grind
    Productive : champion advancing, gates passing
    Stalled : Stall suspected
    Stalled : no champion change in K experiments
    Stalled : marginal composite delta declining
    Reflect : Agent self-reflection [exists as steering command]
    Reflect : re-diagnose against FULL history
    Reflect : must emit different experiment class
    Sweep : Literature sweep [exists as steering command]
    Sweep : search new backbone family
    Sweep : re-seed search from retrieval
    Escalate : Human escalation [partial]
    Escalate : steering console surfaces suggested prompt
    Escalate : notification to technical lead [planned]
    Pivot : Human pivot [exists]
    Pivot : new dataset / new metric emphasis /
    Pivot : accept plateau and ship champion
    Violation : Integrity violation
    Violation : any audit gate hard-fail post-hoc

    Productive --> Stalled : stall detector fires [planned]
    Productive --> Violation : violation detected
    Stalled --> Reflect : automatic, logged
    Reflect --> Productive : new axis found
    Reflect --> Sweep : reflection exhausted local axes
    Sweep --> Productive : new backbone family adopted
    Sweep --> Escalate : sweep returns nothing new
    Escalate --> Pivot : human responds in natural language
    Escalate --> Escalate : no response - campaign continues conservatively
    Pivot --> Productive : steering logged to steering_log.md
    Violation --> Productive : experiment discarded with full reasoning, loop resumes
    Pivot --> [*] : human ends campaign - final bundle emitted
```

**What a reviewer should notice:** (1) escalation is a *ladder*, and the two cheap rungs (self-reflect, literature sweep) run without the human — the founder-reported ablation says high-level steering beat low-level intervention, and low-level human edits actively slowed convergence (paper C.2, single-seed; RE-bench's division of labor independently agrees [B4]); (2) integrity violations bypass the ladder entirely — discard is unconditional and logged, never negotiated with the human; (3) the no-response self-loop at Escalate encodes the honest limit the paper concedes: a human must eventually notice — the system's job is to make noticing cheap (one dashboard glance) and to keep grinding conservatively meanwhile; (4) every transition writes to the steering log, which is both the audit trail and the demo (BRIEF: the steering log is the proof that technical-lead-level humans can run research factories).
