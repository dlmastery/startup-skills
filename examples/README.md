# examples/

`kiln.params.yaml` is a **fictional** filled `params.yaml`. Use it only to see field shape and density.

For a real run: copy `templates/params.yaml`, or let `startup-setup` write `params.yaml` from the founder's description. Never copy Kiln nouns into another idea.
